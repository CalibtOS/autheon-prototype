# Meeting Closure: 2026-06-25 and 2026-06-29

Canonical PRD: [`prd.json`](prd.json)

## Status

The June meeting decisions that are ready to specify are now captured in PRD v1.8:

- Active-tour document uploads are in scope.
- Admin off-channel documents at draft create/edit are in scope.
- Driver uploads and replacements are limited to driver-owned `driver_pwa` documents.
- Daily driver acceptance limits, same-day overlap confirmation, and daily-limit override requests are in scope.
- Admin cancellation requires reason code, driver-facing message, policy cutoff handling, and auditability.
- Operational and cancellation policies are stored in `app_settings`.
- Distance estimation plus manual override is in scope.
- German postal-prefix filtering uses inclusive prefix matching.
- Customer/address master-data notes do not automatically become order-specific tour notes.
- Credit notes, AI/scanner document-quality validation, vehicle type icons, and explicit by-latest scheduling mode are future scope unless the client confirms otherwise.

## Client Decisions Still Open

These remain product decisions, not implementation bugs:

1. Selective driver blocking: granular marketplace/accept block while allowing document corrections, or coarse `Blocked`.
2. Whether `zugelassen / nicht zugelassen` is a required V1 job/vehicle field.
3. Manual direct assign policy and required proof.
4. Whether `daily_job_limit` applies to admin direct assign or only driver marketplace acceptance.
5. Whether marketplace cards show pickup/delivery windows or only detail screens do.
6. Whether date plus time windows are enough, or an explicit by-latest mode is required.
7. Whether pickup and delivery date filters are independent or linked.
8. Final marketplace sort labels/order/defaults.

These are recorded in `production_open_questions`.

## Backend Status Snapshot

As of this note, `autheon-be` contains code paths for the June P0/P1 production themes:

- `daily_job_limit`, accept enforcement, overlap confirmation API, and daily-limit override request flow.
- Active-tour document upload and driver replacement guard for `driver_pwa` only.
- Admin/generated document sources and upload-asset-backed generated PDF persistence.
- Admin cancellation reason/message validation and operational policy cutoff handling.
- Distance calculation fields, manual override, and GraphHopper/no-op routing adapter split.

Remaining backend confidence work is E2E-level proof against PRD acceptance scenarios, especially visibility, authorization, notification routing, and full operational transition audit/status-history coverage.

## Verification

Latest local documentation/prototype checks:

- `node prototype/project/_audit-prototype.mjs` passed.
- `node prototype/project/_verify-seed.mjs` passed.
- `docs/requirements/prd.json` parses as valid JSON.

