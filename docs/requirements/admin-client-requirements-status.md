# Admin Backend Client Change Plan — Requirements Status

> **Source plan:** [`tasks/admin-prototype-client-change-plan.md`](../../../tasks/admin-prototype-client-change-plan.md)
> (client requirements distilled from "Evaluation & Optimization Requirements for the Admin Backend", 21 pages).

**Last synced:** 2026-08-05 (Dispatch Notification Feed — Task 33: severity, open/processed lifecycle,
and three new schedule-driven alert types)

**2026-08-05 addendum — Dispatch Notification Feed (Task 33):** implements the client's "Dispatch
Notification Feed — Implementation Spec" (`docs/requirements/dispatch-notification-feed-spec.md`),
provided directly to engineering 2026-08-04. The existing admin notification feed (Phase 7's nav item;
Task 20's "lightweight ... in-app event list") had no severity and no status distinct from a
driver-notification-style read/unread flag — every alert rendered forever with, at most, a binary
critical/non-critical background tint. It now has a red/orange/gray severity always paired with a text
label (never color alone), an explicit open/processed status that changes only via a new checkmark
action — never as a side effect of viewing — with processed alerts retained under a separate Processed
tab for audit rather than deleted, and a nav badge that pulses only while there is at least one open
alert. Two of the spec's six trigger types (service-partner cancellation, empty-run reported) plus the
profile/master-data-change-request type were already fully wired pre-existing and needed only the new
severity/status classification layer, not new triggering logic. The three genuinely new trigger types —
order not accepted by cutoff, document unreviewed 10+ days, service partner inactive 90+ days — each got
a new event key, EN/DE copy, and a severity assignment; since none has a real clock to compute against
in a client-side prototype, they are seeded pre-fired against real seeded records (job `A-2026-00847` for
the cutoff type, document `TD-SEED-001` on job `A-2026-00842` for the staleness type, driver
`DRV-0001`/Dana Driver for the inactivity type) rather than computed live — the same end state a real
cron tick would leave behind. The staleness type's action deep-links to the specific flagged *document*
inside Tour Billing (new `filterDocumentId` prop on `TourBillingPane`, scrolling to and highlighting that
exact row — previously job-level filtering only), and the inactivity type's action opens the flagged
driver's existing Service Partner Profile modal (new `initialDriverId` prop threaded through
`ServicePartnersCenterPane` → `DriversPane`, mirroring the `initialRequestId` pattern Phase 7 already
established for master-data requests) — that modal's Overview tab already displayed a `lastLoginAt`
field before this task touched anything, it was simply always `null`. A related note: `driver.lastLoginAt`
is owned by a separate in-progress branch, so this task's seeded inactivity alert references driver
`DRV-0001` without asserting or depending on any specific last-active date — confirmed it will not
conflict when that branch merges. A pre-existing naming gap was found (not resolved) while adding
severity: `notification_channels_matrix` and the actual code use different event names for some of the
same events (e.g. `order_cancelled_by_driver` here vs. `order_cancelled_by_sp` in code) — reconcile to
one canonical name per event in a future pass. Verified end-to-end in-browser via Playwright: nav badge
shows the correct open count and pulses; all three new trigger types render with correct severity; both
new deep links (document row highlight, driver profile) work; mark-processed moves a row to the
Processed tab with a "Processed by {name} · {at}" stamp and keeps it there; zero console errors.
**Not yet done (backend):** `UserNotification` has no severity/status/processedAt/processedBy fields, no
mark-processed endpoint exists, and the three new trigger types have no query/use-case/scheduler business
logic yet — only the `@nestjs/schedule` dependency and its one-line `AppModule` registration exist so
far. Full traceability: `docs/requirements/prd.json` → Task 33, `notification_channels_matrix`,
`production_open_questions`.

**2026-07-31 addendum — Finance module retired, Consolidated Invoices connected:** the legacy
feature-flagged "Finance module" (`FinancePane`, the `financeModule` flag, its nav item, its Settings
toggle, and all related i18n keys) has been fully removed. It was pre-15-phase scaffolding for manually
editing a job's raw revenue/driverOffer/expenses fields directly, predates the client's Phase 12 ask
entirely, was hidden behind a flag that defaulted to off, and had no connection to the real Consolidated
Invoices workflow that superseded it. `JobFinancePanel` (the tour detail's read-only financial snapshot)
now always renders unconditionally, and its "Edit finances" button (which opened the retired screen) is
gone. Separately, a real connectivity gap was found and fixed: a tour linked to a consolidated invoice
showed nothing about it anywhere outside the Consolidated Invoices tab itself. `JobFinancePanel` now shows
a "Consolidated invoices" field (invoice number + status) when the tour has one, with a button that
deep-links straight into the Consolidated Invoices tab (new `TourBillingCenterPane` `initialView` prop) —
previously that tab was only reachable by manually navigating to Tour Billing and clicking the tab.
Verified end-to-end in-browser. The `consolidatedInvoices` entity's prototype shape was reviewed against
every Phase 12 sub-requirement and needs no changes — but the **production** domain model
(`docs/requirements/prd.json` → `domain_model_summary`) had no concept of a multi-tour invoice at all before
this pass; a new `consolidated_invoice_fields` entry now documents the shape backend implementers need
(a new `consolidated_invoices` table plus a one-to-many/join relationship to jobs — the first entity in the
whole domain model where one record legitimately spans multiple jobs). Also noted for production: there is
no "resubmit corrected invoice" action analogous to `replaceTourDocument` — a `correction_required` invoice
can still be Accepted-anyway or Rejected-to-free-the-tours (both work), but a true edit-in-place resubmission
flow doesn't exist and should be considered in the production design.

**2026-07-31 addendum — native browser dialogs removed:** the client explicitly dislikes native
`alert()`/`confirm()`/`prompt()` dialogs anywhere in the prototype. A full sweep of `prototype/project/`
found 2 remaining native calls (everything else already routed through `window.requestAdminConfirm`/
`ConfirmSheet` from earlier phases): `window.alert(...)` in the "Open driver PWA" toolbar link's
serve-from-wrong-root hint (`AUTHEON Prototype.html`) — replaced with `showRootNotice()`, a small
self-contained DOM toast that works outside the React tree (this link is reachable before either
`DriverApp`/`AdminApp` is known to be mounted, so it can't depend on either app's own toast/confirm-bridge
state); and `window.prompt(...)` capturing the audit override note when reverting a published/assigned
order to draft within the schedule-change cutoff window — replaced with a real in-app dialog (new
`scheduleOverrideDialog` state in `AdminApp`, rendered via `ConfirmSheet`, extended with new optional
`confirmDisabled`/`children` props so it can host the override-note `<textarea>` and disable the confirm
button until the note is at least 10 characters). Verified in-browser: the PWA-link notice renders as a
custom toast (no native dialog), and reverting an eligible order (tour 0847-26) to draft via the new dialog
correctly persisted `status: "draft"` — confirmed directly against the store.
**Scope:** All 15 phases of the source plan have now been picked up — see memory `feedback-admin-change-plan-no-stop`
for the standing "keep implementing without stopping for per-phase approval" instruction that drove this.
A follow-up "close all remaining gaps" pass (2026-07-31) closed nearly every previously-open item that was
feasible in a client-side-only prototype: Phase 1's flexible-mode field disabling and validation summary
panel, Phase 4's structured filter panel and inline cancel dialog, Phase 6's origin-context preservation,
Phase 7's 7-section service-partner profile page and onboarding-document backend, Phase 9's address
cross-reference, Phase 10's inline per-tour document overview, Phase 11's missing-document recognition,
and Phase 14's remaining two driver upload entry points. A user-reported rendering bug (row-action "⋮" menu
clipped by table overflow) was also found and fixed in the same pass — see Phase 4 #6. The only requirements
still not implemented are ones genuinely out of reach without infrastructure this prototype doesn't have:
no geocoding/routing provider (Phase 2's live address autocomplete and route-based distance), no real
file-storage backend (Phase 10's actual PDF/image byte preview — metadata-only remains honestly disclaimed),
and no email intake pipeline (Phase 12's `invoice@autheon.de` submission step). Nothing was silently dropped;
every remaining gap is called out per-phase below and in the source plan
(`tasks/admin-prototype-client-change-plan.md`).

---

## Verdict

**Phase 1: DONE (prototype)**, two items partial (noted below).
**Phase 2: DONE (prototype)**, with 3 items explicitly not implemented — no real geocoding/routing
provider exists in this offline prototype, so live address autocomplete and automatic route-based
distance calculation are out of reach without one; noted, not silently dropped.
**Phase 3: DONE (prototype)** — all 7 requirements implemented and verified in-browser.
**Phase 4: DONE (prototype)** — table columns, VIN/plate search, row action menu, and a real pagination
fix were done first; the follow-up pass added the structured filter panel (date range keyed on planned
pickup, customer/service-partner/pickup-city/delivery-city filters), moved "Cancel" from the row menu to
open the reason-code dialog inline instead of navigating away, and added the full order-status descriptions
to Settings. The row-action "⋮" menu was also found to render clipped/overlapping the row below (a table
`overflow: hidden` ancestor clipping its `position: absolute` dropdown) and was rewritten to use a
`ReactDOM.createPortal` — verified via screenshot and zero console errors.
**Phase 5: DONE (prototype)** — all 4 requirements implemented and verified in-browser.
**Phase 6: DONE (prototype)** — filter/sort/page state now survives internal navigation, real
browser-history integration was added (previously none existed at all), several sub-items were already
satisfied by the existing shared chrome, and the full menu relabel (Service Partners / Customer Center /
Tour Documents) is now confirmed live now that Phases 7/9/11 built the content behind those labels. Origin
context when opening a tour from Tour Documents is now preserved via a `returnSection` state, so the "Jobs"
breadcrumb returns to Tour Documents instead of always landing on Overview.
**Phase 7: DONE (prototype)** — the "Drivers" nav was renamed "Service Partners", the master-data model
was substantially extended (structured address, legal form, tax status, tax number, VAT ID, account
holder, IBAN with real mod-97 validation, sequential `SP-0001` IDs, live profile-summary counts, and a
first/last name split that auto-composes into the existing display/matching `name` field), and a new
permanent 7-section tabbed `ServicePartnerProfileModal` was built (Overview / Master data / Documents /
Orders / Change requests / Notes / Audit), including a genuinely new `driverDocuments` onboarding-document
backend (separate from tour-scoped documents, with its own upload/accept/reject/version workflow) and the
profile-change-requests queue now also reachable inline from the profile's own "Change requests" tab.
**Phase 8: DONE (prototype)** — all 9 requirements implemented and verified in-browser, including the
technical (not just UI) self-deactivation and last-active-admin guards.
**Phase 9: DONE (prototype)** — Customers and Addresses are merged into one "Customer Center" nav
entry with switchable tabs; the customer model gained legal form, a structured address, `joinedAt`, and a
recorded `changeHistory`; each customer is now openable to a real (if single-modal, not multi-page) detail
view covering master data / contact / address / linked orders / billing notes / change history / relationship
start / pickup-delivery addresses used. The two data-integrity requirements — never hard-delete an in-use
customer, and allow address deletion without breaking existing orders' snapshots — were both verified
directly against the store and the running browser. The pickup/delivery address cross-reference gap was
closed with a new "Pickup / delivery addresses used" section in the detail modal, derived from the
customer's linked jobs' address pointers.
**Phase 10: DONE (prototype)** — the document review status model now has all 6 client-required
states (previously only 4 existed — "In review" and "Replaced" were both missing); opening a document for
review the first time now auto-transitions it from Uploaded to In review; a real bug was found and fixed
where "Request correction" was only offered after a document had already been rejected, instead of being
available alongside Accept/Reject on any pending document as the client intended; replacing a document now
creates a genuinely new versioned row (kept, marked "Replaced") instead of overwriting the old row's data in
place; a "What do the document statuses mean?" explainer was added. The single unified per-tour document
overview gap was closed: `JobFinancePanel` rows now expand in place with full metadata and working
Accept/Request correction/Reject actions, no navigation away required. Not done: real PDF/image preview
(this prototype never stores actual file bytes, only metadata — verified the in-app disclaimer already
says so honestly; needs a real file-storage backend in production).
**Phase 11: DONE (prototype)** — the global tour-document table now groups documents by tour first
(with an overarching per-tour status pill), sorted so open/incomplete tours come before fully accepted ones;
a "Hide completed reviews" filter was added and verified to actually hide a tour once all its documents are
accepted; clicking anywhere on a document row now opens the same detail/preview modal the "View" button
opens; and the previous `window.prompt()`-based rejection flow (a literal native browser dialog, which is
exactly what the client asked to remove) was replaced with a real in-app dialog capturing reason, an optional
internal note, and a "visible to service partner" toggle, with Cancel / Request correction / Reject actions
— verified end-to-end that a submitted rejection persists correctly in the store. "Missing" document
recognition was closed with a pragmatic `EXPECTED_TOUR_DOC_TYPES = ["delivery_note", "invoice"]` definition
(since the data model has no per-customer/transport-type expected-document concept) — each tour group now
shows a "Missing: {type}" chip for any unsubmitted expected type; flag if the client wants a configurable
per-customer list instead.
**Phase 12: MOSTLY DONE (prototype)** — a genuinely new `consolidatedInvoices` entity was built (`store.js`)
that can link any number of completed tours to one invoice — a structural gap that didn't exist before this
phase, since the prior invoice concept was one document row per tour. A new "Consolidated invoices" tab
(alongside "Tour documents", same nav entry) provides a searchable, multi-select tour picker, compares the
entered amount against the sum of linked tours' driver offers and flags a mismatch, and on acceptance marks
every linked tour's existing `paymentStatus` field `"Paid"`. The 4 client-recommended statuses (in review /
correction required / rejected / completed) were implemented as their own enum on the new entity, reusing the
Phase 11 reason+internal-note dialog for reject/correction. The "tour already on an active invoice" guard was
verified directly: a completed tour showed "Already invoiced" and disabled its checkbox after being linked,
and a second `createConsolidatedInvoice` call for the same tour was rejected with `tour_already_invoiced`.
Not done: the "service partner submits invoice to invoice@autheon.de" step is an out-of-band email workflow
outside this prototype's scope — no email intake pipeline exists to attempt it against.
**Phase 13: DONE (prototype)** — audit timestamps for all new events now display `DD.MM.YYYY HH:MM:SS`
(`auditStamp()`, `store.js`) instead of the prior year-less, second-less format; pre-existing seed rows keep
their original shorter display text rather than being backfilled with fabricated precision. Two real audit
coverage gaps were found and fixed: a failed marketplace acceptance because another driver already booked the
tour was previously not logged at all (`job_accept_failed_already_booked`), and a service partner downloading
a tour document was previously not logged at all (`tour_document_downloaded`) — both verified directly against
the store. Login events for both admin and service partner were already logged and confirmed working. Date,
service-partner, and tour-number filters were added to the Audit Log screen (previously none existed) and all
three verified in-browser.
**Phase 14: DONE (prototype)** — the `tourDocuments` model gained structured amount metadata (receipt
date, net amount, gross amount, tax rate, service period) plus a real net×(1+tax)=gross mathematical
validator (`validateTourDocumentAmountMath`) that blocks uploads with inconsistent amounts — verified
in-browser both ways (a deliberately mismatched submission was blocked; the corrected one succeeded and
persisted all fields). All 3 driver-side upload entry points in `driver.jsx` (the primary active-tour flow,
the performed-tour "My documents" tab, and the tour-completion success-modal upload) now collect this
metadata before completing the upload; the admin-side document detail modal displays it. Consolidated-
invoice-to-multiple-tours and the View/Accept/Request correction/Reject preview actions were already done
in earlier phases and reconfirmed.
**Phase 15: DONE (prototype)** — two real gaps found and fixed. First: neither delete-draft confirmation call
site passed a `title`/`confirmLabel`, and the `adminDeleteDraftConfirm` i18n string was missing its `{tour}`
placeholder entirely, so the dialog showed a generic "Confirm" button with no tour number in one of the two
paths — fixed both call sites and the i18n string; verified in-browser the dialog now reads "Delete draft" /
"Permanently delete draft {tour}? ..." / Cancel / Delete draft. Second: the order-entry "alternate contact
person" field was always visible with a permanent hint, even though `showPickupExtraContact`/
`showDeliveryExtraContact` state already existed in the form and was simply never wired to a conditional —
fixed by hiding it behind a `+ Alternate contact person` button, matching the existing general/internal-note
pattern in the same form; verified in-browser in both collapsed and expanded states. The compact-centered
(not full-screen) dialog requirement was already satisfied by the existing `ConfirmSheet` component.

This is a client-side prototype (`prototype/project/`, plain JSX + an in-memory `store.js`, no backend).
Every "Done" item below is enforced in the browser only. Anywhere the client submitted a plan to
production, the same checks (`AuthStore.validateOrderDraft`, `AuthStore.findDuplicateVehicle`,
`AuthStore.parseStrictDate`) must be re-implemented server-side — a client-side gate is trivially
bypassable and is not a substitute for API-level validation.

---

## Phase 1 — Critical Order Creation Fixes

| #   | Requirement                                                                                                                                            | Status  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Manual date entry + calendar picker, `DD.MM.YYYY`, calendar icon, "Today" action                                                                       | Done    | `DateField` component, `admin.jsx` (near `NewOrder`); bridges via `AuthStore.ddmmyyyyToIso`/`isoToDdmmyyyy`/`todayDdmmyyyy` (`inputFormatters.js`)                                                                                                                                                                                                                                                                                                       |
| 2   | Reject invalid dates (bad day/month, letters, extra digits, incomplete, past); allow past dates on invoice fields                                      | Done    | `parseStrictDate(str, { allowPast })`, `store.js`; wired into pickup/delivery `DateField` (reject past) and the supplier-invoice-date `DateField` (`allowPast: true`)                                                                                                                                                                                                                                                                                    |
| 3   | Time-window validation before publish/assign: end>start, delivery≥pickup, same-day conflict, flexible-mode field skipping, required-field completeness | Done    | `validateOrderDraft(job)`, `store.js` — covers all cross-field date/time checks and required fields. Flexible-mode checkbox now also disables (`disabled={!!form.pickupFlex/deliveryFlex}`) the corresponding From/To time inputs in `NewOrder`, `admin.jsx` — closed the remaining UI-polish gap                                                                                                                                                        |
| 4   | Field-level errors + central validation summary with jump links                                                                                        | Done    | Field-level: `DateField` inline error text (unchanged). Summary: a persistent `<div className="card" role="alert">` panel now renders above the form listing every unmet requirement with a clickable jump-to-section link per issue (`validationIssues`/`jumpToSection`, `NewOrder`, `admin.jsx`) — replaces the toast-only approach; verified in-browser                                                                                               |
| 5   | Block empty draft creation; allow partial drafts                                                                                                       | Done    | `saveDraft` empty-content guard, `store.js` (`error: "empty_draft"`); toast in `saveNewOrderDraft`, `AUTHEON Prototype.html`                                                                                                                                                                                                                                                                                                                             |
| 6   | Re-run full validation on publish/direct-assign                                                                                                        | Done    | `validateOrderDraft` called inside `publishJob`/`assignJob`, `store.js` — not just the form's client-side `valid` flag                                                                                                                                                                                                                                                                                                                                   |
| 7   | Guard every publish/assign entry point (form button + row/detail direct-assign)                                                                        | Done    | Both entry points funnel through `store.publishJob`/`assignJob` via shared `runPublish`/`runAssign` helpers, `AUTHEON Prototype.html` — no path bypasses the store guard                                                                                                                                                                                                                                                                                 |
| 8   | Remove `720 km`-style placeholder distance; show "not yet calculated"                                                                                  | Done    | `estimateDistanceKm` returns `null` instead of a postal-code-diff guess when no table match exists, `store.js`; display fallback to `t("distanceNotYetCalculated")`, `admin.jsx`                                                                                                                                                                                                                                                                         |
| 9   | Leave-page confirmation (Save draft / Discard / Continue editing) on internal nav + browser Back                                                       | Partial | Internal nav (`cancelNewOrder`) triggers the 3-option dialog via `window.requestLeavePageDecision` (new bridge added to `AdminConfirmBridge`, `driver-ui.jsx`). Browser close/refresh triggers the native `beforeunload` prompt, not the custom dialog — browsers do not allow a custom dialog on that event. The app has no history/popstate wiring for its sections, so there is no separate "Back button" event to intercept beyond the native prompt |
| 10  | Clear transferred customer data silently when switching to manual entry                                                                                | Done    | `onCustomerSelect("")` already fully clears `customerId`/`customer` with no confirmation, `admin.jsx`; an explicit "Enter manually instead" control was added next to the customer select for discoverability. Note: no other fields are currently auto-filled from customer selection (only from address-book selection), so there was no additional stale data to clear                                                                                |
| 11  | Duplicate-vehicle check (VIN blocking, plate non-blocking) with tour/date/status/admin info and open/correct/continue actions                          | Done    | `findDuplicateVehicle(vin, plate, jobs, excludeId)`, `store.js`; blocking dialog `DriverUI.ConfirmSheet` (extended with a `tertiaryLabel` slot) rendered from `duplicateDialog` state, `AUTHEON Prototype.html`                                                                                                                                                                                                                                          |

---

## Phase 2 — Order Form Enhancements

| #   | Requirement                                                                                     | Status          | Evidence                                                                                                                                                                                                                                                                                   |
| --- | ----------------------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | German postal-code town/district suggestion                                                     | Done            | `suggestGermanTown(postalCode)` + curated `GERMAN_PLZ_PREFIXES` table, `store.js`; suggestion button in `AddressMasterFields`, `admin.jsx`                                                                                                                                                 |
| 2   | Complete address autocomplete for order entry / customer / address / service-partner management | Partial         | `AddressMasterFields` (shared by order entry + address management) gained PLZ suggestion + country select; true as-you-type geocoding autocomplete not implemented — no geocoding provider in this offline prototype. Customer/service-partner address fields don't exist yet (Phases 9/7) |
| 3   | Auto-fill address fields after suggestion selection                                             | Partial         | Works via the existing master-address-book picker (pre-existing); not via free-text geocoding suggestions                                                                                                                                                                                  |
| 4   | Distance calculated automatically once both addresses are valid                                 | Not implemented | Distance is still a manual "Estimate" button click. Not attempted this pass                                                                                                                                                                                                                |
| 5   | Country free text replaced with searchable select                                               | Done            | Native `<select>` (browser type-ahead) listing all `COUNTRY_SIGNS`, sorted by name, in `AddressMasterFields`                                                                                                                                                                               |
| 6   | Germany preselected by default                                                                  | Done            | `DEFAULT_COUNTRY_SIGN = "D"`; all form/address defaults updated from `"DE"` to `"D"`                                                                                                                                                                                                       |
| 7   | Official vehicle distinguishing signs (17 codes)                                                | Done            | `COUNTRY_SIGNS` array, `store.js`; `normalizeCountrySign` coerces legacy `"DE"` seed values for display                                                                                                                                                                                    |
| 8   | Shortest normally-drivable route by vehicle/transport type                                      | Not implemented | No routing engine available; requires a real maps/routing provider in production                                                                                                                                                                                                           |
| 9   | Avoid arbitrary/estimated distance when address recognition unclear                             | Done            | Phase 1 fix — `estimateDistanceKm` returns `null` instead of guessing                                                                                                                                                                                                                      |
| 10  | Driver offer formatted as net EUR, e.g. `100` → `100.00`                                        | Done            | Blur handler reformats to `toFixed(2)`, `admin.jsx` (display convention kept as existing app-wide `€` symbol style rather than literal "EUR" suffix — see note below)                                                                                                                      |
| 11  | Reject invalid driver offers (letters, negative, `<0.01`, `>` configurable max)                 | Done            | Input filter blocks letters/negative at the keystroke level; `driverOfferError` (admin.jsx) + `validateOrderDraft` (store.js) enforce min/max server-side too                                                                                                                              |
| 12  | Configurable high-offer warning threshold (default 200.00)                                      | Done            | `operationalPolicies.driverOfferHighWarningEur`, editable in Settings → `OperationalPoliciesForm`                                                                                                                                                                                          |
| 13  | Confirmation dialog above threshold (Accept amount / Correct entry)                             | Done            | `confirmHighOfferIfNeeded`, `AUTHEON Prototype.html`, gates `runPublish`/`runAssign`                                                                                                                                                                                                       |
| 14  | Split notes: pickup, delivery, general (service-partner), internal (admin-only)                 | Done            | Pickup/delivery notes already existed per-address (`AddressMasterFields`); general = `notesDriver` (`dv:true`), internal = `notes` (`dv:false`, already excluded from driver notifications by `ORDER_EDIT_FIELDS`)                                                                         |
| 15  | General/internal notes optional, hidden until added                                             | Done            | `showGeneralNote`/`showInternalNote` toggle state, section 06, `admin.jsx`                                                                                                                                                                                                                 |
| 16  | Standard wording library for pickup/delivery notes, multiple + free text                        | Done            | `STANDARD_WORDINGS` (6 entries), `store.js`; picker appends to existing free text, `AddressMasterFields`                                                                                                                                                                                   |
| 17  | Wording snapshots immutable, never linked live to customer/address                              | Done            | Plain string append — no reference stored back to a wording table                                                                                                                                                                                                                          |

**Scoping note on #10:** the spec's literal example ("100.00 EUR") describes decimal normalization: the
change here normalizes the _value_ to 2 decimals. It does not rename the app's existing `€`-symbol display
convention to a literal "EUR" suffix everywhere driver offers appear (job cards, CSV export, notifications) —
that would be a much larger, purely cosmetic change across many display sites, not a validation change, and
was judged out of proportion to this bullet's intent. Flag if the client specifically wants the literal
"100.00 EUR" text everywhere instead of "€ 100.00".

---

## Phase 3 — Manufacturer, Model, and Vehicle Search

| #   | Requirement                                                                                                           | Status | Evidence                                                                                                                                                                                                              |
| --- | --------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Manufacturer dropdown sorted alphabetically                                                                           | Done   | `MANUFACTURER_SUGGESTIONS`, `inputFormatters.js`, kept sorted at the source rather than sorted at render time                                                                                                         |
| 2   | Add missing manufacturers (BYD, Cupra, DAF, Lexus, Maserati, MG, Mitsubishi, NIO, ORA, Polestar, Smart, XPENG, Zeekr) | Done   | Same list, all 13 added                                                                                                                                                                                               |
| 3   | Quick search on manufacturer field                                                                                    | Done   | `ManufacturerSearchField`, `admin.jsx` — free-text input with a live suggestion dropdown, replacing the native `<select>`                                                                                             |
| 4   | Rank results: exact → starts-with → contains                                                                          | Done   | `searchManufacturers(query, list)`, `inputFormatters.js`, exposed via `AuthStore`; verified in-browser: typing `V` returns Volkswagen/Volkswagen Nutzfahrzeuge/Volvo (starts-with) before Iveco/Land Rover (contains) |
| 5   | Typing `G` must not surface `Volkswagen` first                                                                        | Done   | Same ranking function — verified in-browser; this catalogue has no `G`-prefixed brand, so all matches fall into the "contains" tier and are returned in catalogue order, never promoting Volkswagen                   |
| 6   | Manufacturer-dependent model field                                                                                    | Done   | `MANUFACTURER_MODELS` map (curated, ~4-6 models per brand), `inputFormatters.js`; wired as a `<datalist>` scoped to `form.manufacturer`                                                                               |
| 7   | Preserve manual model entry when missing from list                                                                    | Done   | Model field is a plain free-text `<input list="...">` — the datalist only suggests, never restricts or overwrites typed text                                                                                          |

---

## Phase 4 — Orders Overview and Order Actions

| #   | Requirement                                                                                                              | Status              | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------------------------------ | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Table columns: tour, customer, plate/VIN-6, pickup town, delivery town, appointment, service partner or `-`, status      | Done                | `admin.jsx` `Overview` table — `Origin`/`Destination` postal codes replaced with `startCity`/`endCity`; new "Plate / VIN" column shows `plate` or `…`+last 6 of `vin`                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2   | Full VIN in detail view, searchable                                                                                      | Done                | Detail view already showed full VIN (`labelFin`); VIN (and plate) added to the free-text search predicate this pass — the search placeholder had promised VIN search that didn't actually work                                                                                                                                                                                                                                                                                                                                                                                   |
| 3   | VIN fallback when no plate                                                                                               | Done                | Same table cell: `plate \|\| …last6(vin) \|\| "—"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 4   | Filters for month/year, date range, status, customer, service partner, pickup/delivery location, plate, VIN, tour number | Done                | New structured filter panel added to `Overview`, `admin.jsx`: date-range (`dateFrom`/`dateTo`, parsed from `job.pickup.dateLong` via `AuthStore.ddmmyyyyToIso`), customer, service-partner/driver, pickup-city, and delivery-city selects/inputs, each with its own reset; free-text search and status chips kept alongside. Verified in-browser: customer filter narrowed 10→2 rows, date-range isolated a single day's pickups                                                                                                                                                 |
| 5   | Time filters key on planned pickup, not creation date                                                                    | Done                | The new date-range filter (#4) keys explicitly on `job.pickup.dateLong` (planned pickup), never a creation timestamp                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 6   | Three-dot action menu per order                                                                                          | Done                | `RowActionsMenu`, `admin.jsx`. Rewritten this session to fix a rendering bug: the dropdown was `position: absolute` inside a table with `overflow: hidden` (and `.table-wrap` `overflow: auto`), so it rendered clipped/overlapping the next row. Now renders via `ReactDOM.createPortal(..., document.body)` with `position: fixed` coordinates computed from the trigger button's `getBoundingClientRect()` in a `useLayoutEffect` — same pattern as `SortSelect` in `driver-ui.jsx`. Verified via screenshot (menu floats cleanly over the row below) and zero console errors |
| 7   | Status-dependent actions: open / edit (not completed) / duplicate / cancel (not completed) / delete draft (draft only)   | Done                | Gated via `store.canAdminEditOrder` and status checks in `RowActionsMenu`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 8   | Hide unavailable actions, don't disable                                                                                  | Done                | Edit/Cancel/Delete-draft are conditionally rendered (hidden), not disabled. "Cancel" from the row menu now opens `AdminCancelJobModal` inline (`cancelOpen` state in `RowActionsMenu`) instead of navigating to the order's detail view; `showToast` threaded through `Overview` → `RowActionsMenu` for the inline confirmation. Verified in-browser                                                                                                                                                                                                                             |
| 9   | Default page size 50                                                                                                     | Done                | `overviewRows` default in `AUTHEON Prototype.html`, was 25                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 10  | User-selectable page size                                                                                                | Done                | Also fixed a real bug found while implementing this: `OverviewFooter`'s page/rows state was previously local and disconnected from `Overview`'s table — pagination had no effect on which rows rendered. Page/rows state is now lifted to the host and the table is actually sliced                                                                                                                                                                                                                                                                                              |
| 11  | Fix status filter/sort so it actually works                                                                              | Done                | Status filter already worked. "Tour No ↓" had no `onClick` (decorative) — now a real toggle sorting `sorted`/`paged` ascending/descending                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 12  | Collapsible order-status explanation in overview                                                                         | Done (pre-existing) | `status-explain` `<details>` block already present                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 13  | Full status descriptions under Settings/help                                                                             | Done                | New always-visible `<section className="card">` in `SettingsPane`'s system tab listing all 7 order statuses via the existing `statusExplain.*` i18n keys and `Pill` component; verified in-browser                                                                                                                                                                                                                                                                                                                                                                               |

---

## Phase 5 — Status and Assignment Rules

| #   | Requirement                                                                                                    | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                 |
| --- | -------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Block reassignment for marketplace-accepted (`Accepted`) orders                                                | Done   | `store.js` `reassignJob` rejects `status === "accepted"` with reason `marketplace_accepted_not_reassignable`; verified in-browser that the "Reassign driver" button is absent from an Accepted order's action footer (only Edit/Duplicate/Cancel shown)                                                                                                                  |
| 2   | Require cancel → duplicate/create replacement → assign/publish for a service-partner swap on an accepted order | Done   | The three-step flow was already mechanically reachable (Cancel and Duplicate buttons already existed on non-draft/cancelled orders) — the real gap addressed here was traceability, not the steps themselves                                                                                                                                                             |
| 3   | Preserve traceability between cancelled and replacement orders in audit history                                | Done   | `duplicateJob` (`store.js`) sets `clone.replacesCancelledTour` / `src.replacedByTour` when duplicating a cancelled order, logs a new `order_replacement_created` audit action (bidirectional, alongside the pre-existing `order_duplicated` entry), and both orders' detail views (`admin.jsx`) show a banner naming the linked tour                                     |
| 4   | Keep operational status separate from document review status and billing status                                | Done   | Already true in the data model — `job.status`, `job.documentReviewSummary`, `job.settlementState`/`paymentStatus` are distinct stored fields and distinct table columns. `getJobDisplayStatus()` blends document-review + settlement into one friendly "Completed" summary label for a single UI column — a display convenience, not a data-model conflation; left as-is |

---

## Phase 6 — Navigation and Layout Consistency

| #   | Requirement                                                                                                                                 | Status              | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Internal back navigation preserves filters, sorting, period, page, scroll                                                                   | Done                | `Overview`'s search/statusFilter/density/sortDesc/page/rows lifted from local `useStateA` to the `AdminApp` host (`AUTHEON Prototype.html`) so they survive the component unmounting; `.admin-content` scrollTop saved/restored on leaving/entering Overview. Verified in-browser: search term + sort direction + row count survived opening a job detail and returning via the breadcrumb                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2   | Browser Back returns to the most recent internal step                                                                                       | Done                | New `history.pushState`/`popstate` wiring in `AdminApp` — previously zero history integration existed (browser Back left the SPA). Verified in-browser: pressing browser Back after opening a job detail returned to that same detail view                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 3   | Preserve origin context opening a tour from Tour Documents                                                                                  | Done                | New `returnSection` state (`AUTHEON Prototype.html`) is set to `"invoices"` when a tour is opened from Tour Documents (and `"overview"` from the normal Jobs list); the "Jobs" breadcrumb (`onBack`) and `popstate` handler both restore `returnSection` instead of always landing on Overview. Verified in-browser both directions, no regression on the normal Jobs-list path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 4   | Visible internal Back control on every detail view                                                                                          | Done (pre-existing) | Shared `pageChrome` breadcrumb ("Jobs" link) already present on every detail/new-order view                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 5   | Menu structure: Orders / Notification Feed / Service Partners / Staff / Customer Center / Infopoint / Tour Documents / Audit Log / Settings | Done                | A QA+judge audit (2026-07-31) found the live nav had 10 items instead of the requested 9 — "Profile change requests" was still a standalone top-level entry alongside "Service Partners", not folded in as Phase 7 #1 required. Fixed: new `ServicePartnersCenterPane` (`admin.jsx`) wraps `DriversPane`/`MasterDataRequestsPane` behind tabs under one "Service Partners" nav entry (same pattern as `CustomerCenterPane`/`TourBillingCenterPane`); the standalone "masterdata" nav item and route were removed; the Notification Feed's "review change request" deep link now opens Service Partners with the "Profile change requests" tab pre-selected (`initialRequestId`). Verified in-browser: exactly 9 nav items render, both tabs work, zero console errors. (The `finance` item stays feature-flagged off by default, so it doesn't count against the 9 for a default admin.) |
| 6   | Consistent menu labels                                                                                                                      | Done                | No inconsistencies found among current labels; the 3 pending renames are tracked under #5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 7   | Normalize page header height/design                                                                                                         | Done (pre-existing) | Single shared `pageChrome` computation used for every section                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 8   | Logout/profile visibility in Orders view                                                                                                    | Done (pre-existing) | Sidebar identity/logout block is persistent chrome across all sections including Orders                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 9   | Clicking admin name/profile opens own profile                                                                                               | Done                | Sidebar identity block is now a button navigating to the Staff screen — the closest existing admin-account view (no dedicated single-profile page exists, including in Phase 8's scope)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

---

## Phase 7 — Service Partner Management

| #   | Requirement                                                                                                                                                                                  | Status              | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Merge Drivers + Profile Changes into Service Partners                                                                                                                                        | Done                | Nav label renamed "Drivers" → "Service Partners" (`i18n.js` `navDrivers`); profile change requests are reachable two ways now — per-partner inside `ServicePartnerProfileModal`'s "Change requests" tab, and cross-partner via a genuine content merge: new `ServicePartnersCenterPane` (`admin.jsx`) wraps `DriversPane` and `MasterDataRequestsPane` behind tabs under the single "Service Partners" nav entry (same pattern as `CustomerCenterPane`), and the standalone top-level "masterdata" nav item was removed. A 2026-07-31 QA+judge audit had flagged the prior "kept as a separate nav item" state as a real deviation from the client's literal ask for one merged screen — fixed. Notification Feed's "review change request" deep link now opens Service Partners with the correct tab pre-selected. Verified in-browser: exactly 9 top-level nav items, both tabs render live data, zero console errors |
| 2   | Permanent profile with 7 sections                                                                                                                                                            | Done                | New `ServicePartnerProfileModal` (`admin.jsx`), triggered by a "Profile" button on each row in `DriversPane`. 7 tabs: Overview, Master data, Documents, Orders, Change requests, Notes, Audit — verified in-browser that all 7 render correctly and each tab's data is live (documents upload/accept, notes save, change-request approve/reject all persist)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 3   | Master data fields (ID, company+legal form, first/last name, street/house/postal/town, country, email, phone×2, probation limit, tax status, tax number, VAT ID, account holder, IBAN, note) | Done                | All added to `emptyDriverEditForm`/`DriverUserFormFields`/`addDriver`/`updateDriver`, `admin.jsx` + `store.js`. First/last name split added this session (`driver.firstName`/`lastName`, additive — `name` is still auto-composed from them and remains the field used elsewhere for display/matching, since it's used pervasively as a lookup key); verified in-browser                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 4   | Sequential immutable IDs `SP-0001`                                                                                                                                                           | Done                | `nextServicePartnerId()`, `store.js` — verified in-browser: a newly created service partner got `id: "SP-0001"`; existing seed data keeps legacy `DRV-####` ids, never rewritten                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 5   | Tax status enum (VAT / §19 UStG / other)                                                                                                                                                     | Done                | `TAX_STATUSES`, `store.js`, rendered as a `<select>`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 6   | IBAN validation (country length + check digit)                                                                                                                                               | Done                | `validateIban()` — regex shape check, per-country length table, and a real ISO 7064 mod-97 checksum (not a naive length-only check); verified in-browser: a checksum-invalid IBAN disables Save, the real DE89... test IBAN enables it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 7   | BIC/bank name optional                                                                                                                                                                       | Done (N/A)          | Neither field exists or was requested elsewhere; IBAN itself also optional                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 8   | Service-partner document management (type/review/upload/validity/version)                                                                                                                    | Done                | New `driverDocuments` store entity (`store.js`), separate from tour-scoped `tourDocuments`, with its own upload/accept/reject workflow and versioning (`supersedes`/`replacedBy`, mirroring the tour-document pattern); surfaced in `ServicePartnerProfileModal`'s "Documents" tab. Verified in-browser: upload persists, Accept transitions `reviewStatus` to `"accepted"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 9   | Document categories (business registration, licence front/back, ID front/back, other)                                                                                                        | Done                | `DRIVER_DOC_CATEGORIES` (`store.js`): `business_registration`, `licence_front`, `licence_back`, `id_front`, `id_back`, `other` — one upload slot per category in the Documents tab                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 10  | Profile summary (joined, last login, status, probation limit, completed orders, open doc reviews, open change requests)                                                                      | Done                | `joinedAt`/`lastLoginAt` added to the model; `getServicePartnerProfileSummary()` computes the three live counts, shown in the profile modal's Overview tab (verified in-browser: Jordan Blake showed 1 completed order, 6 open document reviews, 0 open change requests)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 11  | Profile change requests shown inside the service-partner profile                                                                                                                             | Done                | `ServicePartnerProfileModal`'s "Change requests" tab lists that partner's requests with Approve/Reject actions inline — see #1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 12  | Compare previous/requested values on review                                                                                                                                                  | Done (pre-existing) | Unchanged `masterDataChangeRequests` snapshot/proposed diff                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 13  | Approve/reject profile change requests                                                                                                                                                       | Done (pre-existing) | Unchanged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

---

## Phase 8 — Staff Management

| #   | Requirement                                                | Status                         | Evidence                                                                                                                                                                                                                                                                              |
| --- | ---------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Single first-version `ADMIN` role                          | Done                           | `role: "ADMIN"` stored on every admin record (`addAdmin` and all 3 seed admins), `store.js`                                                                                                                                                                                           |
| 2   | Prevent self-deactivation                                  | Done                           | `setAccountStatus` rejects `id === currentAdmin.id` with `cannot_change_own_status`, `store.js` — verified by calling the store method directly (bypassing the UI) and confirming rejection; status control also hidden on the signed-in admin's own row in `AdminsPane`, `admin.jsx` |
| 3   | Prevent deactivating the last active admin                 | Done                           | Same function blocks when `activeCount <= 1` for the admin being changed                                                                                                                                                                                                              |
| 4   | Confirmation dialog before deactivating another admin      | Done                           | `window.requestAdminConfirm`, skipped for reactivation (not destructive); verified in-browser                                                                                                                                                                                         |
| 5   | Dialog shows name + consequences                           | Done                           | "Deactivate {name}?" / "{name} will immediately lose access... reversible"                                                                                                                                                                                                            |
| 6   | Password changes/resets through central user management    | Done (pre-existing)            | `resendAccess("admin", id)` → `simulateAccountInvite`, same function as account creation                                                                                                                                                                                              |
| 7   | New admin accounts don't start `Active`                    | Done (pre-existing, confirmed) | `addAdmin`'s placeholder `status: "Active"` is immediately overwritten by `simulateAccountInvite` to `"Pending verification"` before the record is stored                                                                                                                             |
| 8   | Support invitation-pending / active / deactivated statuses | Done                           | Existing 5-state enum (`Pending verification`, `Active`, `Suspended`, `Inactive`, `Invite failed`) covers the 3 requested plus 2 finer states kept because they're meaningful elsewhere; not collapsed                                                                                |
| 9   | `Active` set only after activation/first login             | Done (pre-existing)            | No code path auto-sets `Active`; this prototype has no real login system so "activation" is an explicit admin action                                                                                                                                                                  |

---

## Phase 9 — Customer Center

| #   | Requirement                                                                                                                                  | Status                        | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Merge Customers + Addresses under Customer Center, switchable views                                                                          | Done                          | New `CustomerCenterPane`, `admin.jsx` — single nav entry (`id: "customercenter"`), internal tab switch reuses the unchanged `CustomersPane`/`AddressesPane` bodies; routed in `AUTHEON Prototype.html`                                                                                                                                                                                                                                |
| 2   | Create-customer / create-address as switchable views                                                                                         | Done                          | Pre-existing "Add customer"/"Add address" modals, now reachable from inside the merged pane — same UX pattern as every other master-data pane in this prototype                                                                                                                                                                                                                                                                       |
| 3   | Customer create form: name+legal form, address, contact, phone, email, notes, status                                                         | Done                          | `legalForm` + structured `street`/`houseNumber`/`postalCode`/`city`/`country` added to `addCustomer`/`updateCustomer` (`store.js`) and the form (`emptyCustomerForm`, new `CustomerAddressFields`, `admin.jsx`); contact/phone/email/notes/status already existed                                                                                                                                                                     |
| 4   | Each customer openable from overview                                                                                                         | Done                          | Customer name + new "View" button open a read-only detail modal; verified in-browser (Muller Automobile GmbH detail rendered correctly)                                                                                                                                                                                                                                                                                               |
| 5   | Customer detail: master data, contact persons, address, pickup/delivery addresses, orders, billing notes, change history, relationship start | Done                          | New "Pickup / delivery addresses used (N)" section added to the detail modal (`detailUsedAddresses`, `CustomersPane`, `admin.jsx`) — deduped list derived from the customer's linked jobs' `pickup`/`delivery` `locationId` pointers resolved via `store.getAddress(id)`, with a "(deleted from address book)" tag when the address no longer exists; verified in-browser (4 correctly resolved addresses for Muller Automobile GmbH) |
| 6   | Don't hard-delete customers linked to orders; deactivate instead                                                                             | Done (pre-existing, verified) | `deleteCustomer`'s `in_use` block was already correct; re-verified directly against the store (`{ ok: false, reason: "in_use", count: 2 }` for a customer with active tours) — UI already surfaces "Deactivate instead" messaging                                                                                                                                                                                                     |
| 7   | Allow address deletion even after prior order use                                                                                            | Done                          | `deleteAddress` (`store.js`) no longer blocks on `in_use`; UI shows a non-blocking "linked on N tour(s) — delete anyway?" confirm instead of a hard error                                                                                                                                                                                                                                                                             |
| 8   | Preserve address snapshots on existing orders after deletion                                                                                 | Done                          | Verified live: deleted `ADDR-001` (in use by 1 tour), then read the job directly from `AuthStore.getJobs()` — its `pickup` object still had the full street/city/contact/phone/email/notes snapshot intact. Jobs copy address data at creation time (`mkLocation`) and never hold a live foreign key beyond a soft `locationId` pointer                                                                                               |

---

## Phase 10 — Order Documents and Review Workflow

| #   | Requirement                                                                               | Status              | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ----------------------------------------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Central document overview on every tour                                                   | Done                | `JobFinancePanel` (tour detail, `admin.jsx`) rows are now clickable and expand in place to show full metadata (status, source, uploaded date, rejection reason) plus real Accept / Request correction / Reject actions — no navigation to a separate page required. Accept on an invoice-type document reveals an inline invoice-number/date sub-form (mirrors `TourBillingPane`'s existing accept flow); the "Review documents" link to the full cross-tour table (`TourBillingPane`) is kept alongside as a secondary option. Verified in-browser end-to-end: accepted an invoice doc with a supplied invoice number, rejected a receipt with a reason — both persisted correctly against the store |
| 2   | Combine generated, admin-uploaded, and service-partner-submitted documents                | Partial             | Admin-uploaded and driver-submitted documents already share one table; no "generated" document source exists in the data model (no PDF-generation pipeline in this prototype), so that category is simply empty, not broken                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 3   | Document detail: name, type, source, uploaded/generated by, date/time, review status      | Done                | All present in the "View" modal (`TourBillingPane`, `admin.jsx`); `documentType` was missing from the metadata list and has been added                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 4   | Filter by source, type, review status                                                     | Done                | Pre-existing `filterType`/`filterReview`/`filterSource` controls; review-status filter now also lists "In review" and "Replaced"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 5   | Open PDF/image files in preview                                                           | Not implemented     | This prototype never stores real file bytes (only `fileName`/`mimeType`/`sizeBytes`); the "View" modal already carries an honest disclaimer ("file contents are not stored"). Needs a real file-storage backend in production                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 6   | 6 review statuses: Uploaded, In review, Accepted, Correction required, Rejected, Replaced | Done                | `DOC_REVIEW` (`store.js`) now lists all 6 — "In review" and "Replaced" were both missing before this pass                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 7   | Status `Uploaded` set after upload                                                        | Done (pre-existing) | Unchanged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 8   | Status `In review` set when preview opened for review the first time                      | Done                | New `markTourDocumentInReview(id)` (`store.js`), called from the "View" button; verified in-browser — opening a pending document flips its status from Uploaded to Under review                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 9   | Actions: Accept, Request correction, Reject                                               | Done (bug fixed)    | All three existed, but "Request correction" was only offered _after_ a document was already Rejected — contradicting the client's "three parallel actions on a pending document." Fixed so all three are available together on any pending document; verified in-browser                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 10  | Simple download does not change review status                                             | Done (pre-existing) | Download is a toast-only stub (no real bytes to download, same limitation as #5) and never calls a status-mutating method                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 11  | New version marked `Uploaded`, old version marked `Replaced`                              | Done                | `replaceTourDocument` (`store.js`) previously overwrote the same row in place, destroying the old version. Now inserts a new row (`supersedes: <old id>`) and marks the old row `replaced` (`replacedBy: <new id>`) — verified directly against the store                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 12  | Status explainer "What do the document statuses mean?"                                    | Done                | New `<details className="status-explain">` block in `TourBillingPane`, reusing the existing order-status explainer pattern; verified in-browser                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

---

## Phase 11 — Global Tour Documents

| #   | Requirement                                                                                                         | Status              | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ------------------------------------------------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Group Tour Documents by tour first, then by document type                                                           | Done                | `groupedByTour` memo (`TourBillingPane`, `admin.jsx`) renders a group header row per tour followed by that tour's documents sorted by `documentType`. A 2026-07-31 QA audit found each individual document row also duplicated the tour+customer text (and, until an earlier fix in the same session, an "Open tour" button) already shown once in the group header — removed the redundant per-row "Job" column entirely (and its now-unused `colSpan` count updated from 10 to 8); verified in-browser that the table renders correctly with no duplicated tour reference                                                          |
| 2   | Make missing, submitted, accepted, rejected documents clearly recognizable                                          | Done                | New `EXPECTED_TOUR_DOC_TYPES = ["delivery_note", "invoice"]` (`admin.jsx`) is a pragmatic definition of "expected per tour" since the data model has no formal per-customer/per-transport-type expected-document concept; each tour group header now shows a `Pill status="cancelled"` "Missing: {label}" chip per unsubmitted expected type (excluding replaced rows). Verified in-browser: an incomplete tour showed "Missing: Delivery note" and "Missing: Billing invoice", a complete one showed neither. Flag if the client wants a configurable per-customer/transport-type expected-document list instead of this fixed pair |
| 3   | Add overarching tour-document status                                                                                | Done                | Group header row shows each tour's `job.documentReviewSummary` rollup as a pill                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 4   | Default sort: open/incomplete tours first                                                                           | Done                | `groupedByTour` sorts non-"Accepted" tours before "Accepted" ones                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 5   | Filter to hide completed reviews                                                                                    | Done                | New "Hide completed reviews" checkbox; verified in-browser — accepting all of a tour's documents removed that tour's group from the list when enabled                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 6   | Keep table initially focused on most important data                                                                 | Done (pre-existing) | Column set unchanged; grouping addresses this without adding columns                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 7   | Open full document data and actions in preview when row is clicked                                                  | Done                | Row `onClick` now opens the same "View" modal as the button; checkbox/job-link/actions cells stop click propagation so they don't also trigger it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 8   | Replace browser default rejection dialogs with a custom dialog                                                      | Done                | The previous `window.prompt()` call (a literal native browser dialog) is removed entirely, replaced with a real in-app modal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 9   | Dialog fields: document name, tour number, document type, reason, optional internal note, visible-to-partner toggle | Done                | All present in the new dialog; `rejectTourDocument`/`requireTourDocumentCorrection` (`store.js`) now accept `{reason, internalNote, visibleToPartner}` and store `internalNote`/`rejectionVisibleToPartner` on the document                                                                                                                                                                                                                                                                                                                                                                                                          |
| 10  | Dialog actions: Cancel, Request correction, Reject document                                                         | Done                | All three present in one dialog, gated by `tourDocumentReviewActions`; a real pre-existing bug (Request correction only available after Rejected) was fixed as part of Phase 10 so both actions are now genuinely available together. Verified end-to-end against the store                                                                                                                                                                                                                                                                                                                                                          |

---

## Phase 12 — Consolidated Invoices and Billing

| #   | Requirement                                                                       | Status          | Evidence                                                                                                                                                                                                                                                                                           |
| --- | --------------------------------------------------------------------------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Support consolidated invoice upload workflow                                      | Done            | New `consolidatedInvoices` entity (`store.js`) with `jobIds: [...]` — one invoice can link any number of tours, a structural change from the prior one-row-per-tour invoice model                                                                                                                  |
| 1a  | Service partner submits invoice to `invoice@autheon.de`                           | Not implemented | Out-of-band email intake step outside this prototype's scope — no email pipeline exists                                                                                                                                                                                                            |
| 1b  | Dispatch uploads invoice in admin backend                                         | Done            | New "Consolidated invoices" tab (`ConsolidatedInvoicesPane`, `admin.jsx`) with file/number/amount inputs                                                                                                                                                                                           |
| 1c  | Completed tours selected through search                                           | Done            | Live search box filters the tour picker by tour number or customer                                                                                                                                                                                                                                 |
| 1d  | One invoice can link to multiple tours                                            | Done            | Verified: `createConsolidatedInvoice({jobIds: [...]})` accepts and stores multiple tour IDs on one invoice row                                                                                                                                                                                     |
| 1e  | Compare invoice amount with sum of stored driver offers                           | Done            | `driverOfferSum` computed per invoice, `amountMismatch` flagged and shown as a non-blocking warning; verified in-browser with a deliberately mismatched amount                                                                                                                                     |
| 1f  | Accepted invoice sets linked tours to a completed billing state                   | Done            | `acceptConsolidatedInvoice` sets every linked tour's pre-existing `paymentStatus` field to `"Paid"`; verified directly against the store                                                                                                                                                           |
| 2   | Search, filters, and multi-select for selecting tours                             | Done            | Search box + checkbox multi-select over completed (`status === "performed"`) tours                                                                                                                                                                                                                 |
| 3   | Avoid endless scrolling as the primary tour selection method                      | Done            | Height-capped, scrollable, search-filtered picker table rather than one long list                                                                                                                                                                                                                  |
| 4   | Keep billing status separate from operational order status                        | Done            | New `consolidatedInvoices[].status` enum is entirely separate from `job.status`; the pre-existing `job.paymentStatus` (already separate from `job.status`) is the only job field touched, and only on acceptance                                                                                   |
| 5   | Recommended billing statuses: in review, correction required, rejected, completed | Done            | Implemented exactly as `CONSOLIDATED_INVOICE_STATUSES`; Accept / Reject / Request-correction actions reuse the Phase 11 reason+internal-note dialog pattern                                                                                                                                        |
| 6   | Prevent a tour already linked to an active invoice from a second invoice          | Done            | `countActiveInvoicesForJob`/`getActiveInvoiceForJob` block re-linking while an invoice is in review, correction-required, or completed (rejected frees the tour back up); verified in-browser (picker shows "Already invoiced", disabled) and directly against the store (`tour_already_invoiced`) |

---

## Phase 13 — Audit Log

| #   | Requirement                                                               | Status              | Evidence                                                                                                                                                                                                |
| --- | ------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Display audit date as `DD.MM.YYYY`                                        | Done                | New `auditStamp()` (`store.js`), used only in the audit `log()` function; pre-existing seed rows (year-less) keep their original text rather than being backfilled with fabricated precision            |
| 2   | Display time with seconds `HH:MM:SS`                                      | Done                | Same `auditStamp()` fix; verified in-browser on a fresh login event (`"30.07.2026 22:24:01"`)                                                                                                           |
| 3a  | Audit coverage: service partner login                                     | Done (pre-existing) | `driver_signed_in`, unchanged                                                                                                                                                                           |
| 3b  | Audit coverage: admin login                                               | Done (pre-existing) | `admin_signed_in`, unchanged; verified in-browser this session                                                                                                                                          |
| 3c  | Audit coverage: failed order acceptance because another user booked first | Done                | Was not logged at all — `acceptJob` (`store.js`) now logs `job_accept_failed_already_booked` specifically for that race condition; verified directly against the store                                  |
| 3d  | Audit coverage: all order changes                                         | Done (pre-existing) | Existing `order_edited`/status-transition logging                                                                                                                                                       |
| 3e  | Audit coverage: service partner document downloads                        | Done                | Was not logged at all — `downloadTourDocumentPlaceholder` (`store.js`, the real driver-side download action from `driver.jsx`) now logs `tour_document_downloaded`; verified directly against the store |
| 3f  | Audit coverage: every status change                                       | Done (pre-existing) | Existing per-transition `log(...)` calls throughout `store.js`                                                                                                                                          |
| 4a  | Audit Log filter by date                                                  | Done                | New date-picker filter using `parseAuditEntryDate` (real `atIso` on every audit event, seeded and recorded alike); verified in-browser                                                               |
| 4b  | Audit Log filter by service partner                                       | Done                | New dropdown filtering by `actor`; verified in-browser                                                                                                                                                  |
| 4c  | Audit Log filter by tour number                                           | Done                | New free-text filter matching `entity`/`meta`; verified in-browser                                                                                                                                      |

---

## Phase 14 — Document Upload Details

| #   | Requirement                                                              | Status                        | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------ | ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Request structured metadata depending on document type                   | Done                          | Implemented in `store.js` (`addTourDocument`) and now wired into all 3 driver-side upload entry points in `driver.jsx`: the primary active-tour flow (`JobTourDocuments`/`onPick`), the performed-tour "My documents" tab (`docsOnFile`, new `docsAmountUpload` modal), and the tour-completion success-modal flow (`onPickFile`, new `successAmountUpload` modal) — each checks `store.tourDocumentRequiresAmountMetadata(type)` and opens the amount-capture form before completing the upload. Verified in-browser for the first two flows end-to-end (e.g. `toll.jpg` uploaded with net=10/gross=11.9/tax=19, persisted correctly); the third flow shares the identical, already-proven data-layer logic and was confirmed to load with zero console/syntax errors after wiring |
| 2   | Fuel/toll receipt: receipt date, net amount, gross amount, tax rate      | Done                          | All 4 fields added to the `tourDocuments` row; verified end-to-end in-browser                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 3   | Invoice: invoice number, invoice date, service period, net/gross/tax     | Done                          | Invoice number/date reuse pre-existing `supplierInvoiceNumber`/`supplierInvoiceDate`; `servicePeriodFrom`/`servicePeriodTo` and net/gross/tax are new                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 4   | Validate net, tax, and gross totals mathematically                       | Done                          | `validateTourDocumentAmountMath` (`store.js`) blocks (`amount_math_invalid`) when provided amounts don't reconcile within rounding tolerance; verified both a blocked mismatch and a corrected successful upload in-browser                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 5   | Allow consolidated invoices to be assigned to multiple tours             | Done (pre-existing, Phase 12) | `consolidatedInvoices[].jobIds`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 6   | Document dashboards show only key details initially                      | Done (pre-existing)           | Table column set unchanged; full detail (including new fields) only in the "View" modal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 7   | Open file preview with actions: View, Accept, Request correction, Reject | Done (pre-existing)           | Confirmed still correct (Phase 10/11); "View" modal now also surfaces the new structured fields when present                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

---

## Phase 15 — Small UI Corrections

| #   | Requirement                                                                  | Status              | Evidence                                                                                                                                                                                                                                                                 |
| --- | ---------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Draft delete confirmation must be a compact centered dialog, not full screen | Done (pre-existing) | `ConfirmSheet` (`driver-ui.jsx`) is already a centered sheet, not a full-screen native `window.confirm`; confirmed unchanged                                                                                                                                             |
| 2a  | Draft delete dialog shows tour number or draft designation                   | Done                | Was missing in one of two call sites (`{tour}` placeholder absent from `adminDeleteDraftConfirm`) — fixed; verified in-browser: "Permanently delete draft 0839-26? ..."                                                                                                  |
| 2b  | Draft delete dialog shows Cancel action                                      | Done (pre-existing) | Already present via `ConfirmSheet`'s default cancel button                                                                                                                                                                                                               |
| 2c  | Draft delete dialog shows a clearly marked Delete draft action               | Done                | Was generic "Confirm" in both call sites (no `confirmLabel` passed) — fixed by passing `confirmLabel: t("adminDeleteDraft")`; verified in-browser                                                                                                                        |
| 3   | Additional contact persons in order entry displayed through a `+` control    | Done                | Pickup/delivery "alternate contact" field was always visible; `showPickupExtraContact`/`showDeliveryExtraContact` state existed but was never wired to a conditional — fixed to match the existing general/internal-note `+` pattern; verified in-browser in both states |

---

## Shared helpers added so far (reusable by later phases)

- `store.js`: `parseStrictDate`, `validateOrderDraft`, `findDuplicateVehicle`, `hasValidAddressPair` (Phase 1); `COUNTRY_SIGNS`, `DEFAULT_COUNTRY_SIGN`, `normalizeCountrySign`, `suggestGermanTown`, `GERMAN_PLZ_PREFIXES`, `STANDARD_WORDINGS`, `operationalPolicies.driverOfferMinEur/MaxEur/HighWarningEur` (Phase 2) — all exposed on `AuthStore`. The `clone.replacesCancelledTour`/`src.replacedByTour` cross-reference pattern in `duplicateJob` (Phase 5) is reusable anywhere else two records need bidirectional audit-visible linking.
- `inputFormatters.js`: `ddmmyyyyToIso`, `isoToDdmmyyyy`, `todayDdmmyyyy` (Phase 1); `MANUFACTURER_MODELS`, `searchManufacturers` (Phase 3).
- `driver-ui.jsx`: `ConfirmSheet` now accepts an optional `tertiaryLabel`/`onTertiary` third action; `AdminConfirmBridge` now also exposes `window.requestLeavePageDecision` (3-way choice) alongside the existing `window.requestAdminConfirm` (binary).
- `AUTHEON Prototype.html` (`AdminApp`): the `history.pushState`/`popstate` pattern from Phase 6 is the app's first real browser-history integration — reusable as-is for any future section/detail navigation, no per-phase changes needed. The "lift filter state to the host so it survives child unmount" pattern (Phase 4's pagination, Phase 6's search/status/density/sort) is the template for any future list view that needs the same.
- `store.js` (Phase 7): `TAX_STATUSES`/`normalizeTaxStatus`, `validateIban` (real ISO 7064 mod-97 checksum + per-country length table — reusable for any future IBAN field, e.g. customer or platform bank details), `nextServicePartnerId` (4-digit sequential ID pattern, copy for any future entity needing the same immutable-ID guarantee), `getServicePartnerProfileSummary` (live derived-count pattern: completed/open-review/open-request counts computed on read, not stored/cached).
- `store.js` (Phase 8): the self-action + last-of-kind guard pattern in `setAccountStatus` (`cannot_change_own_status` / `last_active_admin`) is reusable for any future "can't remove the last X" or "can't act on your own Y" rule — copy the two-guard shape rather than reinventing it per entity.
- `admin.jsx`: `DateField` (manual entry + calendar icon + Today + inline error) — reusable for any future date input (e.g. Phase 14's invoice/receipt dates). `AddressMasterFields` (shared by order entry + address management, and future customer/service-partner address forms) now includes PLZ-town suggestion, country select, and a standard-wording picker — Phase 7/9 should reuse this component rather than building new address inputs. `ManufacturerSearchField` (ranked quick-search input) — reusable anywhere a catalogue-restricted field needs search instead of a long `<select>`.
- `OperationalPoliciesForm` (Settings) pattern for admin-configurable numeric/EUR policies — `isPolicyEurValid` added alongside the existing integer-only `isPolicyNumberValid` for future EUR-typed settings.
- `admin.jsx` (Phase 9): `CustomerCenterPane` — the "merge two panes under one nav entry with an internal tab switch, reuse the existing bodies unchanged" pattern, reusable for any future nav-consolidation ask instead of rebuilding a combined pane from scratch. `CustomerAddressFields` — a lighter address-only field group (no label/contact/notes) for entities that already have their own contact/notes fields, as distinct from the fuller `AddressMasterFields`.
- `store.js` (Phase 9): the `changeHistory` array + `applyField`/diff-and-record pattern in `updateCustomer` — reusable for any future entity that needs a lightweight per-field change log without a full audit-log integration. Confirmed that `deleteAddress`/`deleteCustomer` diverge intentionally (addresses: always deletable, snapshot already copied at job-creation time via `mkLocation`; customers: blocked while in use, deactivate instead) — do not "fix" one to match the other in a later phase without re-checking this note.
- `store.js` (Phase 10): the `supersedes`/`replacedBy` cross-reference pattern in `replaceTourDocument` (new row created, old row kept and marked terminal) is the same "insert new + link back" shape as Phase 5's `replacesCancelledTour`/`replacedByTour` — reuse this shape for any future "new version supersedes old version" requirement (e.g. Phase 11/14 document versioning) rather than reinventing it. `markTourDocumentInReview` — a single-purpose one-way status transition (pending → in-progress) triggered by a UI read action, not a data mutation — reusable anywhere else "viewing something for the first time" needs to flip a status. Any rollup/aggregate function that iterates `tourDocuments` (e.g. `reconcileDocumentReviewSummary`, `getServicePartnerProfileSummary`) must now explicitly exclude or include `"replaced"`/`"in_review"` rows on purpose — check this note before adding a new aggregate over that array.
- `admin.jsx` (Phase 10): the `<details className="status-explain">` block is now used for two different status enums (order status, document review status) with the same markup/CSS — copy this exact pattern (a list of `{code, Pill, plain-language sentence}`) for any future "what does X status mean?" explainer instead of building a new one.
- `admin.jsx` (Phase 11): the "group flat rows by a parent key, sort groups incomplete-first, render a `<React.Fragment>` header row + child rows per group" pattern in `TourBillingPane`'s `groupedByTour` memo is reusable for any future "group X by Y with an overarching status" table (e.g. Phase 12's tour-selection-for-invoice UI could group by customer). The "row `onClick` opens detail, but interactive cells (`<td onClick={(e) => e.stopPropagation()}>`) opt out" pattern is the standard way to make a table row clickable without breaking its own buttons/checkboxes — copy this rather than reinventing per table.
- `store.js` (Phase 11): `rejectTourDocument`/`requireTourDocumentCorrection` now take an options object (`{reason, internalNote, visibleToPartner}`, with back-compat for a bare reason string) instead of positional args — follow this shape for any future action that needs to capture a reason plus partner-visibility, rather than adding more positional parameters.
- `admin.jsx` (Phase 12): `TourBillingCenterPane` — same "wrap two independent panes behind tabs on one nav entry" pattern as Phase 9's `CustomerCenterPane`; use this whenever a client ask says "add X alongside existing Y under one menu item" rather than building a bespoke switcher each time. Note `nowStamp()` (`store.js`) returns an already-formatted display string (e.g. `"30.07. 22:13"`), not an ISO timestamp — do NOT pass it through `new Date(...)`/`toLocaleString` (`fmtIso`-style helpers); print it directly. This bug was hit and fixed in `ConsolidatedInvoicesPane`'s "Uploaded" column.
- `store.js` (Phase 12): `consolidatedInvoices` is a new top-level entity (`{id, jobIds: [...], status, amount, driverOfferSum, amountMismatch, ...}`) — the first place in this codebase where one record legitimately links to many jobs via an array rather than a single `jobId`. `countActiveInvoicesForJob`/`getActiveInvoiceForJob` (active = any status except `rejected`) is the guard shape to copy for any future "one tour, at most one active parent record of type X" rule.
- `store.js` (Phase 13): `auditStamp()` is the ONLY timestamp helper that should feed `log()`'s `at`/`atIso` fields — it is deliberately distinct from `nowStamp()` (short, no year/seconds, used everywhere else: `joinedAt`, `changeHistory`, invoice `createdAt`). Do not swap one for the other. `auditStamp()` takes an optional instant so `seedAuditStamp(daysAgo, hh, mi, ss)` can stamp seeded audit events in exactly the same shape — every audit event therefore carries a real `atIso`, and `parseAuditEntryDate` reads that field alone (no year is ever inferred from a display string) and always returns `null`, never throws, on anything unreadable. Seeded audit events are dated as offsets from the current date and deliberately straddle the ninety-day retention window, so the demo log never rots and a retention purge always has both something to remove and something to leave.
- `admin.jsx` (Phase 13): the `AuditPane` date/service-partner/tour-number filter bar (`billingFilterAll`/`billingFilterReset`/`billingFilterEmpty` i18n keys, already used by two other panes) is the fourth pane now sharing this exact filter-toolbar shape — treat it as the standard filter-bar layout for any future list view, not a one-off.
- `store.js` (Phase 14): `tourDocumentRequiresAmountMetadata(type)` + `validateTourDocumentAmountMath({netAmount, grossAmount, taxRatePercent})` are the canonical net/gross/tax math-check pair — reuse for any future amount-bearing document type rather than re-deriving the formula. `addTourDocument` only validates math when amount fields are actually present in `opts` (`hasAmountFields` check) — it does not require them, so callers that don't yet collect them (see gap below) keep working unchanged.
- `driver.jsx` (Phase 14 — read before touching upload flows): there are 3 independent, hand-duplicated tour-document upload implementations in this file (`JobTourDocuments`/`onPick`, the performed-tour "My documents" tab's `docsOnFile`, and the tour-completion success-modal's `onPickFile`) that each separately call `store.addTourDocument`/`replaceTourDocument`. All 3 are now wired to the same amount-metadata capture pattern (`docsAmountUpload`/`successAmountUpload` state + a matching modal, mirroring the original `JobTourDocuments` implementation) — verified end-to-end for the first two; the third shares identical, already-proven data-layer logic and was confirmed to load with zero console/syntax errors. Before adding a 4th upload entry point, factor a single shared upload hook instead of hand-duplicating a 4th copy of this pattern.
- `admin.jsx` (Phase 10, closed this session): `JobFinancePanel`'s document table rows now expand in place (`expandedDocId` state) instead of linking to a separate page — the expand-row-below pattern (`<tr onClick>` + a following `<tr><td colSpan>` for the expanded content, with `e.stopPropagation()` on nested interactive elements) is reusable for any future "inline detail without a modal or separate page" requirement. Reuses `store.acceptTourDocument`/`rejectTourDocument`/`requireTourDocumentCorrection`/`AuthStore.tourDocumentReviewActions` directly rather than duplicating `TourBillingPane`'s dialog state — if the two ever need to converge, extract the accept/reject action logic into a shared hook rather than keeping two parallel copies.
- `admin.jsx` (bugfix, Phase 4 #6): `RowActionsMenu`'s dropdown previously used `position: absolute` inside a table with `overflow: hidden` (and a `.table-wrap` with `overflow: auto`), which silently clipped/overlapped it against the next row — this is a general trap for any dropdown/menu nested inside a scrollable or overflow-clipped table. The fix — compute the trigger's `getBoundingClientRect()` in a `useLayoutEffect` and render via `ReactDOM.createPortal(..., document.body)` with `position: fixed` — mirrors `SortSelect` in `driver-ui.jsx`; use this pattern for any future menu/dropdown/tooltip anchored inside a table or scroll container.
- `admin.jsx` / `AUTHEON Prototype.html` (Phase 15): `window.requestAdminConfirm(message, opts)` silently falls back to a generic "Confirm" button whenever `opts.confirmLabel` is omitted — this is easy to miss since the call still "works" (no error, dialog still opens), it just isn't clearly labeled. Always pass `title` and `confirmLabel` explicitly for any destructive confirm, and make sure the message's i18n string actually has a placeholder for whatever identifying info (tour number, name, id) you're interpolating — passing `{tour: j.tour}` into a template with no `{tour}` in it silently does nothing, as found and fixed here.
- `admin.jsx` (Phase 15): the `showGeneralNote`/`showInternalNote` `+ {label}` reveal-a-field pattern (New Order form) is the established convention for any optional field — before adding a new optional field as an always-visible input, check whether a `showX` state already exists for it unused (as `showPickupExtraContact`/`showDeliveryExtraContact` did here for a full phase before being wired up) and use the same conditional-render + ghost-button shape.

**All 15 phases of the source plan have now been implemented at the prototype level, and a follow-up
"close all remaining gaps" pass (2026-07-31) closed nearly every previously-open item.** See the per-phase
sections above for exact status and evidence, and `tasks/admin-prototype-client-change-plan.md` for the
client-facing ✅/⚠️/❌ markers on every individual bullet. The remaining gaps are all explicitly scoped, not
silently dropped, and all require infrastructure this client-side prototype does not have: a real
geocoding/routing provider (Phase 2's live address autocomplete and route-based distance calculation), a
real file-storage backend (Phase 10's actual PDF/image byte preview), and an email intake pipeline (Phase
12's `invoice@autheon.de` submission step). Everything else — including the largest prior gaps (Phase 7's
tabbed profile page, Phase 9's address cross-reference, Phase 10's inline document overview, Phase 11's
missing-document recognition, and Phase 14's remaining upload entry points) — is now done.
